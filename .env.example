# Copy to .env and fill in. .env is gitignored.
# Used by scripts/run_eod.sh and scripts/run_hk_eod.sh to give the launchd-
# triggered `--mode report` step access to the Anthropic API key.

ANTHROPIC_API_KEY=sk-ant-...
